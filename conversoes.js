

// Tipos de dados 
// booleanos

// conversão implicita

// null undefined  -> esses dois valores são tratados como iguais
// "0" == 0 -> a string é convertida em um número antes da comparação 
// 0 == false -> o booleano é convertido em um número ante da comparação 
//"0" == false -> os dois operadores são convertidos em número antes da comparação

// Realizada pelo operador (==) para determinar se dois valores devem ser considerados iguais e também descreve
//o operador de igualdade restrito (===),que não faz conversões ao testar a igualdade.


const numero = 456;
const numeroString = Number("456");

console.log(numero + numeroString);




